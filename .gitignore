node_modules/
.env
*.log
dist/

# Control-plane state is a TRACKED file now (javascript/web/orchestration-config.json);
# ignore the older interim names (leftover runtime files only).
.control-state.json
routing-plan.json
