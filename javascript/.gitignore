node_modules/
.env
*.log
dist/

# The control plane's state now lives in a TRACKED file (web/routing-plan.json) so
# attendees can see/diff/hand-edit it. Still ignore the pre-web-9 hidden dotfile.
.control-state.json
