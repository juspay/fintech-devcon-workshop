node_modules/
.env
*.log
dist/

# The control plane's state lives in a TRACKED file (web/orchestration-config.json)
# so attendees can see/diff/hand-edit it. Ignore the older interim names, which are
# now just leftover runtime files.
.control-state.json
routing-plan.json
